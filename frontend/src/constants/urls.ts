export const aurPackageUrl = (packageName: string): string =>
    `https://aur.archlinux.org/packages/${packageName}/`;

export const packageUrl = (packageName: string): string =>
    `https://archlinux.org/packages/${packageName}/`;
